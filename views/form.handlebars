<header>
    <img class="logo" src="./image/logo.png" alt="spatify-logo" />
</header>

<!-- print spa/salon
from services/deals page, user chooses a card
that card triggers a unique get route based on /:id
which queries "SELECT name, dates, times FROM salons WHERE id=${req.body.id}"
append name to #spa-name, use flatpickr package to import available dates -->
<div class="container">
    <div class="form-style">
        <div id="spa-name">You chose: {{ this.spa - name }}</div>
        <div class="row">
            <div class="input-field col s6">
                Your Name:
                <input id="full_name" type="text" class="validate" />
                <!-- <label for="full-name">Full Name</label> -->
            </div>
            <div class="input-field col s6">
                Date:
                <input type="text" class="datepicker" />
                <!-- <label for="date">Date</label> -->
            </div>
        </div>
        <div class="row">
            {{#each appointment-time}}
            {{!-- make a button dynamically --}}
            {{/each}}

            <button class="time">10:00</button>
            <button class="time">11:00</button>
            <button class="time">12:00</button>
            <button class="time">13:00</button>
            <button class="time">14:00</button>
            <button class="time">15:00</button>
            <button class="time">16:00</button>
            <button class="time">17:00</button>
        </div>

        <form action="/" method="POST">
            <!-- <textarea type="text" name="???"></textarea> -->
            Email:
            <input type="text" id="email" />
        </form>
        <br />

        <button class="submit" type="submit">Submit</button>
    </div>
</div>